# [저장배선 5-B] 환경변수 예시 — 실제 .env 는 커밋하지 않음
#
# APP_MODE: 저장 여부만 갈린다(매칭·리포트는 두 모드 공통, 항상 실작동).
#   미설정/그 외 = showcase(저장 안 함). 'live' 일 때만 conflict_input 저장.
VITE_APP_MODE=showcase

# live 모드에서만 필요. 미설정이면 saveConflictInput 이 저장을 생략한다.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
